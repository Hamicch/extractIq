apiVersion: v1
kind: Service
metadata:
  name: docuflow-api
  namespace: docuflow
  labels:
    app: docuflow-api
spec:
  type: ClusterIP
  ports:
  - port: 3001
    targetPort: 3001
    protocol: TCP
    name: http
  selector:
    app: docuflow-api
